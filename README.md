# فروشگاه لوازم خانگی - Household Appliances E-Commerce

یک سیستم فروشگاهی کامل برای لوازم خانگی با پنل مدیریت قدرتمند، طراحی شده با PHP خالص، MySQL، HTML، CSS و jQuery.

## ویژگی‌ها

### بخش عمومی (فروشگاه)
- 🏠 صفحه اصلی با نمایش محصولات ویژه و جدید
- 🔍 جستجو و فیلتر پیشرفته محصولات
- 📦 سیستم سبد خرید کامل با AJAX
- 🛒 فرآیند خرید ساده و کاربرپسند
- 👤 سیستم احراز هویت کاربران
- 📱 طراحی واکنش‌گرا (Responsive)
- 💳 پشتیبانی از پرداخت در محل

### پنل مدیریت
- 📊 داشبورد با آمار و نمودارها
- 📦 مدیریت کامل محصولات (CRUD)
- 🏷️ مدیریت دسته‌بندی‌ها
- 🛍️ مدیریت سفارش‌ها و تغییر وضعیت
- 👥 مدیریت کاربران
- 🖼️ آپلود تصویر محصولات

### امنیت
- 🔒 رمزگذاری رمز عبور با bcrypt
- 🛡️ محافظت در برابر SQL Injection
- 🔐 توکن CSRF
- ✅ اعتبارسنجی ورودی‌ها

## نیازمندی‌ها

- PHP 7.4 یا بالاتر
- MySQL 5.7 یا بالاتر
- Apache/Nginx
- پسوند PHP MySQLi

## نصب و راه‌اندازی

### 1. کلون کردن پروژه

```bash
git clone https://github.com/MisterCowboy81/HouseholdAppliances.git
cd HouseholdAppliances
```

### 2. ایجاد پایگاه داده

1. وارد phpMyAdmin یا MySQL CLI شوید
2. دیتابیس جدیدی به نام `household_appliances` ایجاد کنید
3. فایل `database.sql` را import کنید:

```bash
mysql -u root -p household_appliances < database.sql
```

یا از phpMyAdmin:
- دیتابیس `household_appliances` را انتخاب کنید
- به تب Import بروید
- فایل `database.sql` را انتخاب و import کنید

### 3. تنظیمات پیکربندی

فایل `includes/config.php` را ویرایش کنید و اطلاعات دیتابیس خود را وارد کنید:

```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', 'your_password');
define('DB_NAME', 'household_appliances');

define('SITE_URL', 'http://localhost/HouseholdAppliances');
```

### 4. دسترسی‌های فولدر

مطمئن شوید فولدر `uploads` قابل نوشتن است:

```bash
chmod -R 755 uploads/
```

### 5. اجرای پروژه

پروژه را در مسیر وب سرور خود قرار دهید (مثلاً `htdocs` یا `www`) و به آدرس زیر بروید:

```
http://localhost/HouseholdAppliances/public/index.php
```

## اطلاعات ورود پیش‌فرض

### پنل مدیریت
- آدرس: `http://localhost/HouseholdAppliances/admin/`
- نام کاربری: `admin`
- رمز عبور: `admin123`

⚠️ **هشدار امنیتی**: حتماً رمز عبور پیش‌فرض را بعد از نصب تغییر دهید!

## ساختار پروژه

```
HouseholdAppliances/
├── admin/                  # پنل مدیریت
│   ├── index.php          # داشبورد
│   ├── products.php       # مدیریت محصولات
│   ├── categories.php     # مدیریت دسته‌بندی‌ها
│   ├── orders.php         # مدیریت سفارش‌ها
│   └── ...
├── public/                 # بخش عمومی فروشگاه
│   ├── index.php          # صفحه اصلی
│   ├── products.php       # لیست محصولات
│   ├── cart.php           # سبد خرید
│   ├── checkout.php       # تسویه حساب
│   └── ...
├── includes/              # فایل‌های PHP مشترک
│   ├── config.php        # تنظیمات
│   ├── db.php            # اتصال به دیتابیس
│   ├── functions.php     # توابع کمکی
│   ├── User.php          # کلاس کاربران
│   ├── Product.php       # کلاس محصولات
│   ├── Cart.php          # کلاس سبد خرید
│   ├── Order.php         # کلاس سفارش‌ها
│   └── Category.php      # کلاس دسته‌بندی‌ها
├── assets/                # فایل‌های استاتیک
│   ├── css/              # فایل‌های CSS
│   ├── js/               # فایل‌های JavaScript
│   └── images/           # تصاویر سایت
├── uploads/              # فایل‌های آپلود شده
│   ├── products/         # تصاویر محصولات
│   └── categories/       # تصاویر دسته‌بندی‌ها
└── database.sql          # فایل SQL پایگاه داده
```

## ویژگی‌های فنی

### Backend
- **PHP خالص** بدون استفاده از فریمورک
- **معماری OOP** برای کدهای تمیز و قابل نگهداری
- **Prepared Statements** برای امنیت
- **Session Management** برای مدیریت کاربران
- **Pattern Singleton** برای اتصال دیتابیس

### Frontend
- **طراحی مدرن** با CSS3
- **Responsive Design** برای موبایل و تبلت
- **jQuery** برای تعاملات AJAX
- **Font Awesome** برای آیکون‌ها
- **Grid و Flexbox** برای لِی‌اوت

### Database
- **طراحی نرمال شده** (Normalized)
- **Foreign Keys** برای یکپارچگی داده‌ها
- **Indexes** برای بهبود عملکرد
- **UTF-8 Encoding** برای پشتیبانی کامل از فارسی

## استفاده

### افزودن محصول جدید

1. وارد پنل مدیریت شوید
2. به بخش "محصولات" بروید
3. روی "افزودن محصول جدید" کلیک کنید
4. فرم را پر کرده و ذخیره کنید

### مدیریت سفارش‌ها

1. به بخش "سفارش‌ها" بروید
2. وضعیت هر سفارش را از منوی کشویی تغییر دهید
3. برای مشاهده جزئیات، روی "جزئیات" کلیک کنید

### تغییر تنظیمات سایت

تنظیمات کلی سایت در فایل `includes/config.php` قرار دارد:
- نام سایت
- آدرس سایت
- تنظیمات آپلود
- تنظیمات امنیتی

## پشتیبانی و توسعه

### گزارش مشکلات
برای گزارش مشکلات یا پیشنهادات، از بخش Issues در GitHub استفاده کنید.

### مشارکت
1. Fork کنید
2. یک branch جدید بسازید (`git checkout -b feature/AmazingFeature`)
3. تغییرات را commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. Push کنید (`git push origin feature/AmazingFeature`)
5. Pull Request ایجاد کنید

## لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## توسعه‌دهنده

پروژه توسط تیم توسعه ایجاد و نگهداری می‌شود.

## تشکر

از تمامی کسانی که در توسعه این پروژه مشارکت کرده‌اند، صمیمانه تشکر می‌کنیم.

---

**نکته**: این پروژه برای اهداف آموزشی و نمایشی طراحی شده است. برای استفاده در محیط production، حتماً اقدامات امنیتی بیشتری انجام دهید.