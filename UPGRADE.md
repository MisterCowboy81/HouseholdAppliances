# راهنمای آپگرید و تغییرات جدید

این فایل شامل تمام بهبودها و تغییرات اعمال شده در پروژه است.

## 📋 فهرست تغییرات

### 1. ✅ ساختار و دسترسی سایت

#### صفحه اصلی در ریشه
- ✅ فایل `index.php` در ریشه برای دسترسی مستقیم به سایت
- ✅ هدایت خودکار به `public/index.php`
- ✅ دیگر نیازی به تایپ `/public/` نیست

#### تنظیمات Apache
- ✅ فایل `.htaccess` با تنظیمات امنیتی و بهینه‌سازی
- ✅ فشرده‌سازی فایل‌ها (Compression)
- ✅ کش مرورگر (Browser Caching)
- ✅ محافظت از فایل‌های حساس
- ✅ صفحات خطای سفارشی (404, 500)

### 2. 🎨 طراحی و رابط کاربری

#### انیمیشن‌ها
- ✅ `fadeIn`: ظاهر شدن نرم المان‌ها
- ✅ `slideInRight`: اسلاید از راست
- ✅ `pulse`: انیمیشن ضربان برای بج‌های تخفیف
- ✅ Smooth scroll برای تمام صفحه

#### کارت‌های محصول
- ✅ افکت hover با بزرگ‌نمایی تصویر
- ✅ سایه پویا هنگام hover
- ✅ گرادیانت شفاف روی کارت
- ✅ بج تخفیف با انیمیشن pulse

#### دکمه‌ها
- ✅ گرادیانت‌های زیبا
- ✅ افکت موج (Ripple effect)
- ✅ سایه‌های پویا
- ✅ انیمیشن در hover

#### هیرو (Hero Section)
- ✅ گرادیانت مدرن بنفش
- ✅ الگوی نقطه‌ای پس‌زمینه
- ✅ انیمیشن متن
- ✅ سایه روی متن

#### فرم‌ها
- ✅ افکت focus با سایه و حرکت
- ✅ تغییر رنگ border در hover
- ✅ آیکون برای select box
- ✅ انیمیشن‌های نرم

#### آلرت‌ها
- ✅ گرادیانت برای هر نوع
- ✅ آیکون خودکار
- ✅ انیمیشن ظاهر شدن

### 3. 📱 طراحی واکنش‌گرا (Responsive)

#### موبایل (تا 768px)
- ✅ لِی‌اوت یک ستونی برای پروفایل
- ✅ هدر قابل انعطاف
- ✅ منوی عمودی
- ✅ دکمه‌های تمام عرض
- ✅ فرم‌های بهینه شده

#### تبلت (768px - 1024px)
- ✅ Grid دو ستونی
- ✅ فضای مناسب بین المان‌ها
- ✅ تصاویر بهینه

#### دسکتاپ (بالای 1024px)
- ✅ Grid سه یا چهار ستونی
- ✅ حداکثر عرض 1200px
- ✅ فضاگذاری مناسب

### 4. 🔐 امنیت و جداسازی نقش‌ها

#### جداسازی مدیر و مشتری
- ✅ مدیران نمی‌توانند محصول بخرند
- ✅ مخفی کردن سبد خرید برای مدیر
- ✅ مخفی کردن دکمه "افزودن به سبد" در تمام صفحات
- ✅ پیام مناسب در صفحه جزئیات محصول
- ✅ بلاک کردن دسترسی به checkout و cart

#### بهبود امنیت
- ✅ محافظت از فایل‌های .htaccess
- ✅ غیرفعال کردن directory browsing
- ✅ Header‌های امنیتی (X-Frame-Options, etc.)
- ✅ CSRF token در تمام فرم‌ها

### 5. 📍 سیستم مدیریت آدرس

#### دفترچه آدرس
- ✅ کلاس `Address` برای مدیریت آدرس‌ها
- ✅ صفحه `addresses.php` برای مدیریت آدرس‌ها
- ✅ افزودن آدرس جدید
- ✅ حذف آدرس
- ✅ تنظیم آدرس پیش‌فرض
- ✅ نمایش تمام آدرس‌های ذخیره شده

#### جدول دیتابیس
```sql
CREATE TABLE user_addresses (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    title VARCHAR(50),
    full_name VARCHAR(100),
    phone VARCHAR(20),
    province VARCHAR(50),
    city VARCHAR(50),
    address TEXT,
    postal_code VARCHAR(20),
    is_default TINYINT(1),
    ...
)
```

#### یکپارچه‌سازی با Checkout
- ✅ انتخاب از آدرس‌های ذخیره شده
- ✅ پرکردن خودکار فرم با آدرس انتخابی
- ✅ استفاده از آدرس پیش‌فرض
- ✅ لینک به صفحه مدیریت آدرس‌ها

### 6. 🎯 صفحات جدید

#### صفحه موفقیت سفارش (order-success.php)
- ✅ طراحی زیبا با آیکون موفقیت
- ✅ نمایش جزئیات سفارش
- ✅ شماره سفارش، تاریخ، مبلغ
- ✅ اطلاعات ارسال
- ✅ مراحل بعدی سفارش
- ✅ دکمه‌های مشاهده سفارشات و ادامه خرید

#### صفحه خطا 404
- ✅ طراحی مدرن با گرادیانت
- ✅ آیکون جستجو
- ✅ پیام دوستانه
- ✅ دکمه بازگشت به صفحه اصلی

#### صفحه خطا 500
- ✅ طراحی مشابه 404
- ✅ آیکون هشدار
- ✅ پیام خطای سرور

### 7. 🔧 بهبودهای Backend

#### کلاس Order
- ✅ متد `getByOrderNumber()` برای جستجو با شماره سفارش
- ✅ بهبود مدیریت تراکنش‌ها

#### توابع کمکی
- ✅ `formatDate()` برای فرمت تاریخ
- ✅ بهبود `timeAgo()`
- ✅ توابع امنیتی CSRF

#### کلاس Address (جدید)
- ✅ CRUD کامل برای آدرس‌ها
- ✅ مدیریت آدرس پیش‌فرض
- ✅ اعتبارسنجی داده‌ها
- ✅ بررسی مالکیت

### 8. 📊 بهبود پروفایل کاربر

#### صفحه پروفایل
- ✅ نمایش اطلاعات کاربر
- ✅ ویرایش پروفایل
- ✅ تغییر رمز عبور
- ✅ تاریخچه سفارشات
- ✅ دکمه دسترسی به دفترچه آدرس
- ✅ جداسازی بهتر بخش‌ها

### 9. 📝 مستندات

#### فایل‌های جدید
- ✅ `database_update_addresses.sql` - آپدیت دیتابیس
- ✅ `UPGRADE.md` - راهنمای آپگرید
- ✅ بروزرسانی README.md

## 🚀 نحوه اعمال تغییرات

### برای پروژه‌های موجود:

1. **آپدیت دیتابیس**:
```bash
mysql -u root -p household_appliances < database_update_addresses.sql
```

2. **پاک کردن کش مرورگر**:
- Ctrl + Shift + Delete
- یا Ctrl + F5 برای رفرش کامل

3. **بررسی فایل config.php**:
- مطمئن شوید `SITE_URL` به درستی تنظیم شده

4. **تست قابلیت‌های جدید**:
- دسترسی از ریشه سایت
- ثبت آدرس جدید
- ثبت سفارش
- تست صفحات خطا (404, 500)

## 🎨 استایل‌های CSS جدید

### کلاس‌های انیمیشن
```css
.fade-in          /* ظاهر شدن نرم */
.slide-in-right   /* اسلاید از راست */
```

### افکت‌های دکمه
```css
.btn::before      /* افکت موج */
```

### انیمیشن‌ها
```css
@keyframes fadeIn
@keyframes slideInRight
@keyframes pulse
```

## 🐛 رفع مشکلات

### مشکلات احتمالی و راه‌حل:

1. **صفحه اصلی کار نمی‌کند**:
   - بررسی کنید فایل `index.php` در ریشه وجود دارد
   - بررسی کنید `.htaccess` فعال است

2. **آدرس‌ها ذخیره نمی‌شوند**:
   - مطمئن شوید جدول `user_addresses` ایجاد شده
   - بررسی کنید فایل `includes/Address.php` وجود دارد

3. **انیمیشن‌ها کار نمی‌کنند**:
   - کش مرورگر را پاک کنید
   - فایل CSS جدید را بارگذاری کنید

4. **صفحات خطا نمایش داده نمی‌شوند**:
   - بررسی کنید `.htaccess` به درستی تنظیم شده
   - مطمئن شوید فایل‌های `404.php` و `500.php` وجود دارند

## 📈 بهبودهای آینده (پیشنهادی)

- [ ] سیستم نظرات و امتیازدهی محصولات
- [ ] لیست علاقه‌مندی‌ها (Wishlist)
- [ ] رهگیری سفارش
- [ ] پرداخت آنلاین
- [ ] چند زبانه
- [ ] پنل تحلیلی پیشرفته
- [ ] اعلان‌های push
- [ ] چت آنلاین

## 🤝 مشارکت

برای گزارش مشکلات یا پیشنهاد بهبودها:
1. Issue جدید در GitHub ایجاد کنید
2. یا Pull Request ارسال کنید

---

**تاریخ آپدیت**: نوامبر 2025  
**نسخه**: 2.0
