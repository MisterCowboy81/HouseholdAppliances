# راهنمای نصب و راه‌اندازی

## پیش‌نیازها

قبل از نصب، مطمئن شوید که موارد زیر را نصب کرده‌اید:

1. **XAMPP** یا **WAMP** یا **LAMP** (برای PHP و MySQL)
2. **PHP 7.4+**
3. **MySQL 5.7+**
4. **مرورگر وب** (Chrome، Firefox، Safari، Edge)

## مراحل نصب گام به گام

### گام 1: دانلود پروژه

پروژه را از GitHub دانلود کنید یا کلون کنید:

```bash
git clone https://github.com/MisterCowboy81/HouseholdAppliances.git
```

یا فایل ZIP را دانلود و Extract کنید.

### گام 2: انتقال فایل‌ها

فایل‌های پروژه را در پوشه مناسب وب سرور قرار دهید:

- **XAMPP (Windows)**: `C:\xampp\htdocs\HouseholdAppliances`
- **XAMPP (Linux/Mac)**: `/opt/lampp/htdocs/HouseholdAppliances`
- **WAMP**: `C:\wamp\www\HouseholdAppliances`

### گام 3: ایجاد دیتابیس

#### روش 1: استفاده از phpMyAdmin

1. مرورگر را باز کنید و به `http://localhost/phpmyadmin` بروید
2. روی تب "New" یا "جدید" کلیک کنید
3. نام دیتابیس را `household_appliances` وارد کنید
4. Collation را `utf8mb4_unicode_ci` انتخاب کنید
5. روی "Create" یا "ایجاد" کلیک کنید
6. دیتابیس جدید را انتخاب کنید
7. به تب "Import" یا "وارد کردن" بروید
8. فایل `database.sql` را از پوشه پروژه انتخاب کنید
9. روی "Go" یا "اجرا" کلیک کنید

#### روش 2: استفاده از MySQL CLI

```bash
# ورود به MySQL
mysql -u root -p

# ایجاد دیتابیس
CREATE DATABASE household_appliances CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# خروج
exit

# Import کردن فایل SQL
mysql -u root -p household_appliances < database.sql
```

### گام 4: پیکربندی اتصال دیتابیس

فایل `includes/config.php` را باز کنید و تنظیمات زیر را ویرایش کنید:

```php
// اطلاعات دیتابیس خود را وارد کنید
define('DB_HOST', 'localhost');
define('DB_USER', 'root');           // نام کاربری MySQL
define('DB_PASS', '');               // رمز عبور MySQL (در XAMPP خالی است)
define('DB_NAME', 'household_appliances');

// آدرس سایت خود را وارد کنید
define('SITE_URL', 'http://localhost/HouseholdAppliances');
```

**نکته مهم**: اگر پروژه را در پوشه دیگری قرار داده‌اید، `SITE_URL` را مطابق آن تغییر دهید.

### گام 5: تنظیم دسترسی‌ها (Linux/Mac)

اگر از Linux یا Mac استفاده می‌کنید:

```bash
cd /path/to/HouseholdAppliances
chmod -R 755 uploads/
```

### گام 6: راه‌اندازی وب سرور

1. **XAMPP**: Apache و MySQL را از کنترل پنل XAMPP استارت کنید
2. **WAMP**: WampServer را اجرا کنید و منتظر بمانید تا آیکون سبز شود

### گام 7: دسترسی به سایت

مرورگر را باز کنید و به آدرس زیر بروید:

```
http://localhost/HouseholdAppliances/public/index.php
```

## ورود به پنل مدیریت

برای ورود به پنل مدیریت:

1. به آدرس زیر بروید:
   ```
   http://localhost/HouseholdAppliances/admin/
   ```

2. از اطلاعات زیر برای ورود استفاده کنید:
   - **نام کاربری**: `admin`
   - **رمز عبور**: `admin123`

⚠️ **هشدار امنیتی مهم**: بعد از اولین ورود، حتماً رمز عبور را تغییر دهید!

## تغییر رمز عبور ادمین

برای تغییر رمز عبور admin:

1. وارد MySQL شوید:
   ```sql
   mysql -u root -p household_appliances
   ```

2. رمز عبور جدید را با کد زیر تنظیم کنید:
   ```sql
   UPDATE users 
   SET password = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi' 
   WHERE username = 'admin';
   ```
   (این رمز عبور `admin123` است - بعداً از پنل تغییر دهید)

## عیب‌یابی مشکلات رایج

### مشکل 1: صفحه خالی یا خطای 500

**راه‌حل**:
1. خطاها را فعال کنید (در `includes/config.php`):
   ```php
   error_reporting(E_ALL);
   ini_set('display_errors', 1);
   ```
2. فایل error_log Apache را بررسی کنید

### مشکل 2: خطای اتصال به دیتابیس

**راه‌حل**:
1. مطمئن شوید MySQL در حال اجرا است
2. اطلاعات دیتابیس در `includes/config.php` را بررسی کنید
3. دیتابیس را با نام صحیح ایجاد کرده‌اید

### مشکل 3: تصاویر نمایش داده نمی‌شوند

**راه‌حل**:
1. دسترسی‌های پوشه `uploads/` را بررسی کنید:
   ```bash
   chmod -R 755 uploads/
   ```
2. مطمئن شوید `SITE_URL` در config صحیح است

### مشکل 4: CSS/JS لود نمی‌شود

**راه‌حل**:
1. `SITE_URL` را در `includes/config.php` بررسی کنید
2. مسیر نصب را با `SITE_URL` تطبیق دهید

### مشکل 5: نمی‌توانم محصول اضافه کنم

**راه‌حل**:
1. اندازه فایل آپلود را در `php.ini` افزایش دهید:
   ```ini
   upload_max_filesize = 10M
   post_max_size = 10M
   ```
2. Apache را restart کنید

## پیکربندی‌های پیشرفته

### تنظیم اندازه آپلود فایل

در `includes/config.php`:
```php
define('MAX_FILE_SIZE', 5 * 1024 * 1024); // 5MB
```

### فعال کردن HTTPS

اگر SSL نصب کرده‌اید:

1. در `includes/config.php`:
   ```php
   define('SITE_URL', 'https://yourdomain.com');
   ini_set('session.cookie_secure', 1);
   ```

### تنظیم ایمیل

برای ارسال ایمیل (نیاز به پیکربندی بیشتر دارد):
- از PHPMailer یا SwiftMailer استفاده کنید
- تنظیمات SMTP را اضافه کنید

## آماده‌سازی برای Production

قبل از انتقال به سرور واقعی:

1. **خطاها را غیرفعال کنید**:
   ```php
   error_reporting(0);
   ini_set('display_errors', 0);
   ```

2. **رمز دیتابیس قوی تنظیم کنید**

3. **HTTPS را فعال کنید**

4. **نسخه پشتیبان منظم بگیرید**:
   ```bash
   mysqldump -u root -p household_appliances > backup.sql
   ```

5. **فایروال و امنیت سرور را پیکربندی کنید**

## پشتیبانی

اگر مشکلی در نصب داشتید:

1. فایل‌های log را بررسی کنید
2. نسخه PHP و MySQL را بررسی کنید
3. از بخش Issues در GitHub سوال بپرسید

## بروزرسانی

برای بروزرسانی پروژه:

1. نسخه پشتیبان از دیتابیس و فایل‌ها بگیرید
2. فایل‌های جدید را جایگزین کنید (بجز `uploads/` و `includes/config.php`)
3. اگر تغییرات دیتابیس وجود دارد، آنها را اعمال کنید

---

موفق باشید! 🎉
